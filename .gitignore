# ── OS junk ──────────────────────────────────────────────────────────────────
.DS_Store
Thumbs.db
desktop.ini

# ── Editor / IDE ─────────────────────────────────────────────────────────────
.vscode/
.idea/
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# ── AI / Agent tooling ───────────────────────────────────────────────────────
.agents/
.gemini/
.cursor/
.aider*
*.aider*

# ── Node (if ever added) ─────────────────────────────────────────────────────
node_modules/
npm-debug.log*
yarn-debug.log*
yarn-error.log*
dist/
.env
.env.*

# ── Claude Code (auto-generated, repo-local) ─────────────────────────────────
.claude/worktrees/
.claude/plans/
.claude/launch.json
*.local.json

# ── Raw GTFS source data (large, not part of the web app) ────────────────────
# Built JSON files (stops.json, trips.json, rail-shapes.json) ARE committed.
# Raw GTFS source files (*.txt, *.zip) and subdirs are NOT.
*.zip
/data/rail_gtfs/
/data/*.txt
scripts/data/

# ── Build / temp ─────────────────────────────────────────────────────────────
*.log
*.tmp
*.bak

# ── Diagnostic / analysis capture files (large, local-only) ─────────────────
scripts/*.jsonl
scripts/audit-feeds-report.json
scripts/live-accuracy-*.summary.json
artifacts/
