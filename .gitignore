# ── OS junk ──────────────────────────────────────────────────────────────────
.DS_Store
Thumbs.db
desktop.ini

# ── Editor / IDE ─────────────────────────────────────────────────────────────
.vscode/
.idea/
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# ── AI / Agent tooling ───────────────────────────────────────────────────────
.agents/
.gemini/
.cursor/
.aider*
*.aider*

# ── Node (if ever added) ─────────────────────────────────────────────────────
node_modules/
npm-debug.log*
yarn-debug.log*
yarn-error.log*
dist/
.env
.env.*

# ── Claude Code worktrees (auto-generated, repo-local) ───────────────────────
.claude/worktrees/
.claude/plans/
*.local.json

# ── Raw GTFS source data (large, not part of the web app) ────────────────────
# Built JSON files (stops.json, trips.json, rail-shapes.json) ARE committed.
# Raw GTFS source files (*.txt, *.zip) and subdirs are NOT.
*.zip
/data/rail_gtfs/
/data/*.txt

# ── Build / temp ─────────────────────────────────────────────────────────────
*.log
*.tmp
*.bak

# ── Diagnostic / analysis capture files (large, local-only) ─────────────────
scripts/*.jsonl
scripts/audit-feeds-report.json

# ── Orphaned / duplicate directories ─────────────────────────────────────────
metrolivemap/

# ── Removed integrations archive (reference only, not served) ────────────────
sidecar/
